## 2026-06-16 17:51 记忆补充

### 架构整理与文件调整
- 已将 `memory/WORKSPACE_ARCHITECTURE_MAP.md` 改为中文说明，并补充当前架构、保留/归档模块、能力调用总线、`market_analysis.prose` 定位等内容。
- 已将旧/过渡代码归档到 `workflows/bak/` 与 `python_wrapper/bak/`，并添加 README 说明。
- `python_wrapper/__init__.py` 已调整为导出当前架构能力：`run_market_analysis_ai` 与 `SkillCaller`，不再导出旧 `workflow.py/stage_connectors.py`。
- 已验证 `import python_wrapper`、`python_wrapper.__version__ == 2.0.0`、核心 Python 文件 `py_compile`，并确认活跃目录不再依赖旧 `workflow.py/stage_connectors.py`。

### 调用总线定位纠偏
- 复杂市场分析的自主调度应交给 `strategy-orchestrator`，主 Agent 负责用户入口、领域判断和最终解释。
- `strategy-orchestrator` 不是只输出 plan 就结束，而应负责完整编排循环：计划、调度、观察证据、反思、重排、决定是否补查/换工具/追问。
- 专业执行由 `data-agent`、`analysis-agent`、`report-agent` 或具体 skills 完成。
- `market_analysis.prose` 定位为市场分析方法论、任务规范、质量门禁参考，不作为确定性流程控制器。
- Python wrapper 只保留 tool bridge、event/SSE bridge、旧接口兼容，不承担自主决策。

## 2026-06-16 18:14 记忆补充

### 用户授权
- 用户明确表示：`strategy-orchestrator` 是我创建的，我可以自主决定它的 `AGENTS.md`，让它承担需要承担的职责。
- 用户要求继续整体规划和落地，不需要等待进一步确认。

### 下一步待执行
- 重新规划自主 Agent 架构。
- 修改 `strategy-orchestrator` 的 `AGENTS.md` / 相关说明，使其成为真正的自主调度大脑。
- 同步调整 `market_analysis.prose` 的定位说明。
- 更新架构地图，避免再把 Prose 或 Python Wrapper 误写成“调用总线大脑”。
