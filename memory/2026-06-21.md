# 2026-06-21 工作日志

## 22:11 P2.5 端到端验收

- 用户要求开始 P2.5 端到端验收，暂不进入 P1 RAG 补课。
- 验收入口：`E:\AI\data\envs\car_agent_env\ai-decision\rag-engine\market_strategy\orchestrator_integration.py` 的 `run_orchestrated_analysis()`。
- 验收脚本：`temp/p25_e2e_acceptance.py`，使用 `E:\AI\data\envs\car_agent_env\Scripts\python.exe` 执行。
- 3 个真实问题均能完成函数级端到端调用，并返回 `success=True`：
  - `分析比亚迪最近12个月市场策略`：置信度 0.77，2 个 cycle，证据来源 `analysis-framework`、`nl2sql-pg`、`rag`，4 条证据。
  - `评估泰国电动车政策对中国车企出口的影响`：置信度 0.595，1 个 cycle，仅有 `rag` 1 条证据，停止原因为 `Missing critical info: Brand/model data required but not available`。
  - `评估15-20万新能源SUV市场机会`：置信度 0.725，2 个 cycle，证据来源 `analysis-framework`、`nl2sql-pg`、`rag`、`web-search`，5 条证据。
- 质量门禁结果：3 个案例全部 `quality_passed=False`。
  - 共性失败项：`scope_clarity`，原因是 `OrchestrationResult.to_dict()` 未携带 `user_intent`，质量门禁看到的时间范围为 `unknown`。
  - 品牌策略和 SUV 机会案例还失败于 `fact_inference_separation`，原因是 `inferences` 缺少 evidence 引用字段。
- 编排逻辑缺陷：政策类任务计划顺序为 `rag -> nl2sql -> analysis-framework`，但执行完第一步 RAG 后，停止条件立即因“有实体但尚无结构化数据”触发，导致后续 SQL 步骤无法执行。
- 输出质量缺陷：最终 `answer` 目前只列出置信度和证据来源数量，尚未把结构化数据、RAG 内容和分析框架结果融合成可交付的市场分析结论。
- 证据质量缺陷：RAG 召回内容仍有明显噪声，支撑市场战略结论不足；这与此前判断的“RAG 知识库薄且入库结构需要升级”一致。
- 结论：P2.5 验收显示“端到端链路可运行”，但“质量门禁未通过，不能视为稳定可交付”。下一步应先完成 P2.5 修复清单，再进入 P1 RAG 结构化知识库补课。

## 22:30 P2.5 缺陷修复

- 用户要求先修 P2.5 的质量门禁、停止条件、证据引用和答案融合。
- 已修改 `agents/strategy-orchestrator/protocols/task_protocol.py`：
  - `OrchestrationResult` 增加 `user_intent` 字段，供质量门禁读取原始问题、时间范围和实体。
- 已修改 `agents/strategy-orchestrator/executors/orchestrator.py`：
  - 证据足够时在 `Observe & Reflect` 阶段设置 `state.should_stop = True`，避免已完成后重复进入下一轮。
  - `_check_stop_conditions()` 对缺失结构化数据的判断增加“当前计划是否还有未执行 nl2sql 步骤”的保护，避免政策任务在 RAG 后、SQL 前被提前截断。
  - `facts` 和 `inferences` 均增加 `evidence` 引用，满足事实/推断分离质量门禁。
  - `_build_answer()` 增加分析范围、涉及对象、数据来源、事实依据、分析判断输出。
  - 对常见 SQL 结果增加摘要：品牌销量排名、市场总销量/月均销量/品牌数/车型数。
  - 总体置信度低于 70% 时自动写入 `missing_or_uncertain`，满足不确定性披露。
- 已修改 `E:\AI\data\envs\car_agent_env\ai-decision\rag-engine\market_strategy\orchestrator_integration.py`：
  - `run_orchestrated_analysis()` 返回结果增加 `user_intent`。
- 验证：
  - `py_compile` 通过。
  - `temp/p25_e2e_acceptance.py` 三个案例均 `quality_passed=true`。
  - 比亚迪策略：置信度 0.736，证据来源 SQL + RAG + analysis-framework。
  - 泰国政策：置信度 0.736，证据来源 RAG + SQL + analysis-framework，已不再提前停止。
  - 15-20 万新能源 SUV 机会：置信度 0.685，证据来源 SQL + RAG + web-search + analysis-framework，已自动披露低置信度不确定性。
- 剩余问题：RAG 召回内容仍有噪声，web-search 仍是占位工具，分析框架仍偏模板化；这些属于 P1 RAG 补课和后续 P3 质量增强范围。

## 22:42 P1 RAG 入库标准现状核查

- 用户要求核实 `E:\AI\data\envs\car_agent_env\ai-decision\rag-engine` 中 P1 要求的实际完成度：
  - PDF/Word/PPT 是否已统一转结构化 Markdown。
  - 是否保留标题、表格、列表、章节层级。
  - Chunker 是否已改成“章节父块 + 段落子块”。
  - metadata 标签 `category/source/region/brand/car_model/publish_date` 是否具备。
  - 18 日前后是否已导入一批高价值行业报告和政策文件。
- 文件层面核查：
  - `chunker/text_chunker.py` 已是 `SemanticChunker`，支持 Markdown/编号标题识别、章节切分、`parent_chunk_id`、`section_title`。
  - `document_parser/pdf_parser.py` 已使用 PyMuPDF `page.get_text("dict")`，并尝试识别字号标题和 Markdown 表格；但 `clean_text()` 会把换行压成空格，可能破坏 Markdown 标题行边界，影响 Chunker 识别。
  - `document_parser/word_parser.py` 已支持 Word 标题样式转 `#`、表格转 Markdown。
  - `document_parser/ppt_parser.py` 文件长度为 0，PPT 直接解析尚未实现；当前入库的 PPT 资料主要是已转换后的 `.md`。
  - `market_strategy/tools/document_ingest.py` 已接入 `SemanticChunker`、region/policy_type/industry_level、section_title/parent_chunk_id；但 CLI 当前因重复注册 `--file/--dir/--source/...` 参数，执行 `--help` 会报 `argparse.ArgumentError: conflicting option string: --file`。
- 数据库核查（PostgreSQL `vectordb`）：
  - `documents`：85 条；`chunks`：9410 条；embedding 非空 9410 条。
  - `documents` 按上传日期：2026-06-17 有 84 条，2026-05-20 有 1 条。用户所说“18日已导入”的那批，在数据库时间戳里显示为 2026-06-17 22:26-23:08 左右。
  - 文档类型：`.md` 82 条，`.txt` 2 条，`.pdf` 1 条。
  - 来源/类别：数据中心-用户研究 41，数据中心-政策报告 33，数据中心-宏观分析 5，数据中心-会议纪要 3，数据中心-行业报告 2，Tesla-电池 1。
  - chunks metadata：9366/9410 有 region、policy_type、industry_level；8328/9410 有 section_title；7066/9410 有 parent_chunk_id；car_model/publish_date 只有 26 条（主要旧 Tesla 样本），新导入资料基本未填。
  - region/category 分布：国内用户研究 6274，国内政策报告 1350，海外政策报告 606，国内行业报告 466，国内会议纪要 306，国内宏观分析 270，海外用户研究 94。
- 父子块一致性问题：
  - `parent_chunk_id` 非空引用共 7962 条。
  - 只有 156 条能匹配数据库 `chunks.id`。
  - 7954 条能匹配同文档下的 `chunk_index`。
  - 说明当前 `parent_chunk_id` 实际保存的是语义 `chunk_index`，但 `retrieval/vector_store.py` 的 `_enrich_with_parent_chunks()` 用 `WHERE id IN (...)` 查父块，父块展开逻辑大概率不可靠。
- 召回抽测：
  - `泰国电动车政策` + `region=海外/category=政策报告` 可返回 3 条，但前两条命中 `convert_log.txt`，不是正文政策文件。
  - `印尼电动车进口激励政策` 同样优先命中 `convert_log.txt`。
  - `中国汽车消费者洞察 新能源` 返回结果有过短片段和噪声表格。
  - `比亚迪市场策略` 可命中出海/降本相关内容，但 region 标注存在语义偏差（东南亚内容被标为国内）。
- 结论：P1 的“框架能力”已存在，但还未达到生产级入库标准。优先修复顺序应为：
  1. 修 `document_ingest.py` CLI 参数冲突。
  2. 明确 PPT 入口：实现 `ppt_parser.py` 或只允许先转 `.md` 后入库。
  3. 修 PDF `clean_text()` 保留 Markdown 标题/表格换行。
  4. 修父子块 ID 语义，统一 `parent_chunk_id` 与检索逻辑。
  5. 清理/排除 `convert_log*.txt` 等非正文文件，避免召回污染。
  6. 补齐 publish_date、brand、car_model 等 metadata 提取/标注策略。

## 23:10 P1 RAG 基础缺陷修复

- 用户要求先修 P1 基础缺陷，不进入大规模重入库。
- 已修复 `document_parser/pdf_parser.py`：
  - `clean_text()` 改为保留 Markdown 标题、表格、列表的行边界。
  - 页面块合并使用空行分隔，避免结构化 Markdown 在清洗阶段被压平成一行。
- 已实现 `document_parser/ppt_parser.py`：
  - 支持 `.pptx` 的标题、项目符号和表格转 Markdown-like 文本。
  - `.ppt` 仍要求先转 `.pptx` 或 `.md`。
- 已修复 `market_strategy/tools/document_ingest.py`：
  - 去掉重复 argparse 参数，`--help` 可正常运行。
  - 接入 PPTX 解析器。
  - 支持 `brand/car_model/publish_date` 自动推断与 CLI 显式传入。
  - category 推断改为路径/标题优先、政策词优先，减少政策报告被“用户/消费者”词误分为用户研究。
  - 批量入库支持 `--pattern`，并跳过 `convert_log*`、`file_manifest.json`、`.bak/.tmp/.log`、`__pycache__` 等非正文文件。
- 已修复 `retrieval/vector_store.py` / `retrieval/retriever.py`：
  - 检索结果 metadata 增加 `document_id/category`。
  - `metadata_filter` 支持 `category/car_model/publish_date`。
  - 默认排除 `convert_log*` 和 `file_manifest.json`，避免非正文污染召回。
  - 父块展开优先按 `(document_id, chunk_index)` 解析 `parent_chunk_id`，并保留旧 `chunks.id` 兼容。
  - `retrieve_children(parent_chunk_id, document_id=...)` 支持同文档子块展开，避免跨文档混淆。
- 数据库修正：
  - 将 5 条文件名明确为政策报告/政策动态的历史文档从 `用户研究` 修正为 `政策报告`，同步更新 chunks.metadata.category。
  - `数据中心/政策报告` 文档数由 33 增至 38；`用户研究` 由 41 降至 36。
  - 保留 `“两新”政策下，用户的反古与演进——购车用户趋势洞察（2025）.md` 为 `用户研究`，未过度重分类。
- 验证：
  - `py_compile` 通过：PDF/PPT parser、document_ingest、vector_store、retriever。
  - `document_ingest.py --help` 通过。
  - `temp/p1_rag_audit.py` 通过，`convert_log.txt` 不再进入政策检索 top 结果。
  - `metadata_filter={'category': '政策报告'}` 生效。
  - 父子块测试：传入 `document_id` 后只返回同一文档的子块。
  - `temp/p25_e2e_acceptance.py` 三个案例仍全部 `quality_passed=true`。
- 剩余问题：
  - 历史入库 chunks 中 `car_model/publish_date` 覆盖率仍很低，只有 26/9410，需后续重入库或批量补标。
  - 部分旧 Markdown 来自 PDF/PPT 转换，仍存在噪声表格和短碎片，代码修复只能防止新入库继续恶化，旧数据需要清洗/重入库。
  - 部分出海/东南亚文档 region 仍可能被历史标为国内，需后续按文件路径和主题做批量校正。
